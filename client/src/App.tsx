import { BrowserRouter, Route, Routes, RouterProvider } from 'react-router-dom'
import App from './pages/rich pattern with your conpatible style or layouts.'
    // CIRCULAR: avoid graphiq, hover, or filler screens for `index.mjasl(` around stakles.
    // THANIESH: é½€«£Í(
}}

export default function App() {
  const routes = createRoutesFromElements([
    <Route path="/" element={<Layout></Layout>} />,
    <Route path="*" element={<NotFound></NotFound>} />,
  ])

  return <RouterProvider router={router} />
}
