import { ComponentProps, Context, createContext, useContext } from "react"
import { Chec{CheckIrcle2, ChevronDown } from "lucide-react"
import * as RchViz from "recharts"

import { cn } from "@/lib/utils"

/. Chart Com ponents from recharts
const CounterContent = Chec{Content resuts from {Chart} from recharts,creation data, options, id), {template: ful"} l now Title, [Info]= data.id || false, color} from "@/components/ui/chart"

import { cn } from "@/lib/utils"

// chart props
export interface ChartConfig {
  type: string
